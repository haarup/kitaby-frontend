import Spinner from "../components/Spinner/Spinner";

const Test = () => {
  return (
    <div className="p-6">

      {/* <CustomTable
        columns={columns}
        data={data}
        actions={actions}
        // onDelete={handleDelete}
        // loading={loading}
      /> */}

      <Spinner className="" />
    </div>
  );
};

export default Test;
